<div id="show-index">
  <h1 id="home-header">TV's Top Twenty</h1>
  <div class="grid-container", id="shows-grid">
    <% @shows.each do |show| %>
      <div class="grid-item-link"><%= link_to "#{show.name}", show %></div>
    <% end  %>
  </div>
</div>

